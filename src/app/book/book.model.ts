export class Book {
  constructor(
    public id?: number,
    public name?: string,
    public author?: string,
    public cost?: string,
    public isbn?: string,
    public attachmentId?: number,
  ) {
  }
}
